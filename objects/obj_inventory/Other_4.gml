// TODO
// check if inventory has item, despawn item on room entry 

if (array_length(global.inventory) > 0)
{
    for (var _i = 0; _i < array_length(global.inventory); _i++)
    {

        // TODO
        // destroy obj 

    } 
}